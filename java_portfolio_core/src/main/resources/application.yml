spring:
  application:
    name: java-portfolio-core
  datasource:
    url: jdbc:postgresql://localhost:5432/us_stock_db
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
server:
  port: 8080

portfolio:
  trade:
    # 部位合併容忍度，可選值: STRICT (2%), DEFAULT (5%), RELAXED (10%)
    merge-tolerance: DEFAULT
